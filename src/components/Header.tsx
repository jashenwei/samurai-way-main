import React from "react";
import logo from "./logo.png";

export const Header = () => {
    return (
        <header className='header'>
            <img src={logo} alt = ''/>
        </header>
    );
};
